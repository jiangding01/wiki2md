# 真机验收清单

> e2e（`node test/e2e.js`）锁定的是**我们对页面结构与接口形态的当前理解**；
> 上游平台改版只有真实线上页面能暴露。本清单即为此存在——全部用真实页面过检，不用 fixture。
>
> **节奏**：
> - 每两周全量过检一次；
> - 动过 `src/adapters/` 或 `src/core/` 后，过对应分组；
> - 发版打 tag 前全量过检。
>
> **记录**：发现的问题当场进 GitHub issue（现象 + 页面 URL；能复现的把脱敏 HTML 存入
> `test/fixtures/`），不留在脑子里。每次过检在文末记录表补一行。

## Confluence（真实 wiki 实例）

- [ ] 单页导出 .md：标题层级、表格、代码宏完整，Obsidian 打开无残渣
- [ ] 开「导出评论」：划线评论呈 `==高亮==[^n]` 脚注锚定原文；页面评论进文末「💬 评论」区，嵌套回复层级正确
- [ ] 含鉴权图片的页面 + 图片本地化：ZIP 解包后 md **离线**打开不裂图
- [ ] 页面树导出（≥2 层子孙页）：ZIP 目录镜像页面层级，导出报告无意外失败项
- [ ] 页面树 + 「导出评论」开关：每页评论随树导出

## 飞书文档

- [ ] 接口精配成功：徽章显示「精配」；标题、富文本（粗斜体/行内代码/链接）、嵌套列表、表格、代码块（含语言）完整
- [ ] 评论导出：划线评论以 quote 锚定为脚注，全文评论进文末，@提及可读
- [ ] 回退链路：DevTools 里 block 掉 `client_vars` 请求后重新分析 → 自动回退滚动采集，徽章回「实验性」，告警明示原因
- [ ] 图片本地化：ZIP 解包后离线不裂图

## 通用兜底抽查

- [ ] 任选一个当周真实读过的普通文章页导出：正文可读，无脚本/导航/推荐位残渣

## 过检记录

| 日期 | 版本 | 结果 | 问题（issue 链接） |
| --- | --- | --- | --- |
